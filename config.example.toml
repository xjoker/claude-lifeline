# claude-lifeline 配置文件
# 复制到 ~/.claude/claude-lifeline/config.toml 使用
# 顶部一组（context/five_hour/seven_day/edit_stats）默认 true，按需关闭
# 「可选块」一组默认 false，需要时主动启用

[display]
context        = true   # context window 块
five_hour      = true   # 5h quota 块
seven_day      = true   # 7d quota 块（同时控制 Sonnet 子块——超速时才显示）
edit_stats     = true   # 代码改动量 +X -Y（仅当 session 有增删时渲染）

# 可选块——默认关闭，避免升级后 UI 静默变化
subscription   = false  # 从 OAuth 凭证读取订阅徽章（MAX·20x / MAX·5x / PRO / FREE 等）
seven_day_opus = false  # Opus 7d 子配额块（O:U/P%!，仅在 Opus 用量超速时出现）
extra_usage    = false  # 月度付费扩容池块（账户未开通扩容时自动隐藏）

# 颜色切换阈值（可选；缺省使用下列默认值）
# 校验规则：yellow 必须 < red，且都在 [0, 100]；不合法时该对回退默认
[thresholds]
ctx_yellow_at       = 60.0   # ctx >= 该值 → 黄
ctx_red_at          = 70.0   # ctx >= 该值 → 红

# 5h / 7d quota 独立配置（默认 7d 比 5h 更宽松，因为 7d 窗口更长）
five_hour_yellow_at = 75.0
five_hour_red_at    = 90.0
seven_day_yellow_at = 80.0
seven_day_red_at    = 90.0

# 超速容差（%）：used > pace + tolerance 才算超速。0 = 严格（任何超出即告警）
pace_tolerance      = 0.0
